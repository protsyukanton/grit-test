1. Создается константа массива с 4 элементами.
2. Затем запускается цикл "for", который будет выполняться, пока есть элементы в массиве.
3. С задержкой в 3 секунды будет выводиться в консоль с помощью условия (тернарного оператора)
"Good" и значение элемента массива, если элемент больше 13 и "Bad", если меньше или равно 13.

Результат:

Bad: undefined
Bad: undefined
Bad: undefined
Bad: undefined


Данный код будет работать, если объявить переменную "i" через "let", а не "var".
Bad: 10
Bad: 12
Good: 15
Good: 21


